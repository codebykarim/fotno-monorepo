// Toggle integrated payments via Stripe Connect on/off without removing
// the underlying UI, routes, or services. Flip to true once the feature
// is live.
export const STRIPE_CONNECT_ENABLED = false;
