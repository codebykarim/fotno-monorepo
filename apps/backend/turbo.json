{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "PORT",
        "MONGODB_URI",
        "NODE_ENV",
        "BETTER_AUTH_SECRET",
        "BETTER_AUTH_URL",
        "RESEND_API_KEY"
      ],
      "outputs": ["dist/**"]
    }
  }
}
