{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "turbo run build",
        "installCommand": "pnpm install",
        "framework": "express"
      }
    }
  ]
}
