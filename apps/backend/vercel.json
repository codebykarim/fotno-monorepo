{
  "version": 2,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.ts"
    }
  ],
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "pnpm install jest --save-dev --recursive --filter=backend --filter=@repo/logger"
}
